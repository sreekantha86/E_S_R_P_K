@model ems.domain.GST
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script>
    function GotoPreviosList()
    {
        location.href = '@Url.Action("Index","GST")';
    }
</script>
<div class="row">
    <!-- left column -->
    <div class="col-md-12">
        <!-- general form elements -->
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">GST Rates</h3>
            </div>
            <!-- /.box-header -->
            <!-- form start -->
            @using (Html.BeginForm())
            {
                @Html.HiddenFor(model=>model.gstId)
                @Html.Partial("_form", Model)
                <!-- /.box-body -->
                <div class="box-footer">
                    <button type="submit" class="btn btn-default">Cancel</button>
                    <button type="button" class="btn btn-default" onclick="GotoPreviosList()">Previous</button>
                    <button type="submit" class="btn btn-info pull-right">Update</button>
                </div>
            }

        </div>
        <!-- /.box -->
    </div>
</div>