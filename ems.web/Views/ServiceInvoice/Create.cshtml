@model ems.domain.ViewModels.ServiceIncome

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <div class="box box-danger">
            <div class="box-header with-border">
                <h3 class="box-title">Service Invoice - Create</h3>
            </div>
            @using (Html.BeginForm())
            {
                @Html.Partial("_headpart", Model)
                <!-- /.box-body -->
                @*@Html.Partial("_gridpart", Model)*@
                <div class="box-footer">
                    <button type="submit" class="btn btn-default">Cancel</button>
                    <button type="button" class="btn btn-default" onclick="GotoPreviosList()">Previous</button>
                    <button type="submit" class="btn btn-info pull-right">Save</button>
                </div>
            }            
        </div>
        <!-- /.box -->
    </div>
</div>